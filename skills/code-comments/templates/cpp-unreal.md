# C++, Unreal

Functions:

```cpp
/**
 * <what it does or returns>
 * @param <Name> <meaning, units, valid range>
 * @return <what it returns, and when>
 */
```

Properties shown in the editor:

```cpp
/**
 * <what the value does>
 * <its units and range>
 */
UPROPERTY(EditAnywhere)
float <Name>;
```
