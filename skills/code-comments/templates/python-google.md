# Python, Google Style

```python
def <name>(<arg>):
    """<What it does, in one line.>

    <More facts, one sentence per line.>

    Args:
        <arg>: <meaning, units, valid range>.

    Returns:
        <What it returns, and when.>

    Raises:
        <Error>: <When it is raised.>
    """
```
